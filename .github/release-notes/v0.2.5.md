HRouter Desktop v0.2.5

- 主界面改为稳定的左侧导航，配置中心、Agent 管理、公告、更新、帮助和设置入口更集中，顶部操作区更简洁。
- HRouter.net 公告与服务直接在桌面应用内展示，不再提供跳出应用的外部打开按钮。
- 设置页只保留语言、主题、Agent 显示、窗口行为和关于信息，修改后自动保存。
- 修复 HRouter `/v1` 重定向导致的连通性误报，检测会请求实际的模型服务端点并显示 HTTP 状态与耗时。
- 移除不符合产品定位的终端工作台及其 PTY/xterm 依赖，保留既有的系统原生终端调用能力。
