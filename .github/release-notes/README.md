# Release notes

Every stable release must include a non-empty Markdown file named after its tag,
for example `v0.2.4.md`. Include at least one bullet item describing a user-visible
change. The release workflows reject missing, empty, or mismatched notes.
