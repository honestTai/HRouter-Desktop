HRouter Desktop v0.2.13

- 精简 HRouter 的 Codex 模型选择与映射：默认保留 GPT-5.4、GPT-5.5、GPT-5.6、GPT-6 四个系列，每个系列选择当前 Key 实际可用的一个模型，保留手动指定的默认模型。
- 修复思考强度只剩“关闭／高”的问题，恢复多档等级配置；有对应 Codex 模型能力信息时，优先保留该模型完整的原生档位。
- 为缺少模型能力信息的路由提供兼容配置选项，实际接受的思考等级以对应上游服务为准。

升级后请在 HRouter 中重新保存 Codex 供应商配置，再完全退出并重启 Codex，使模型目录和思考等级生效。
